/**
 * Main barrel export for ts-simple fixture.
 */

export { User, Product, type UserInput } from './types';
export { formatName, calculateTotal } from './utils';
export { fetchUser, createUser } from './api';
