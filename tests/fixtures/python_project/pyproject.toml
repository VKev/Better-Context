[project]
name = "python-project"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["requests>=2.0"]

[project.scripts]
python-project = "python_project.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
